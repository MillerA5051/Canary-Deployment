# FILE: k8s/overlays/canary/kustomization.yaml
# PURPOSE: Kustomize overlay that takes the base config and patches it
#          to create the canary deployment with fault injection enabled.
#
# WHAT TO WRITE HERE:
#   - apiVersion: kustomize.config.k8s.io/v1beta1
#   - kind: Kustomization
#   - bases: point to ../../base
#   - patches: reference canary-patch.yaml
#   - images: override the product-service image tag with the canary build
#     (the cd.yml workflow updates this tag automatically on each push)
